# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
build/
dist/

# --- Virtual environments ------------------------------------------------
.venv/
venv/
env/
ENV/

# --- OS junk --------------------------------------------------------------
.DS_Store
Thumbs.db

# --- Agent / IDE scratch (do NOT commit) ---------------------------------
.atl/

# --- Local user data (per-machine secrets, journal, overrides) -----------
# scraper_config.json holds an optional API-key override; never commit.
scraper_config.json
# Local action journal; never commit (privacy + size).
arkos_history.jsonl

# --- Downloaded media (regenerated by the scraper; never commit) ---------
*.mp4
*.avi
*.mkv
*.mov
*.webm
*.mpg
*.mpeg
